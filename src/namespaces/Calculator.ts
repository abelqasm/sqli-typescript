namespace Calculator {
    export class BasicCalculator {
        constructor() {};
        add(a:number, b: number){ a + b};
        sub(a:number, b: number){ a - b};
        divise(a:number, b: number){ a / b};
        multip(a:number, b: number){ a * b};
    }
}
